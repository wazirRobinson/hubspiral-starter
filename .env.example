NODE_ENV=development
PORT=3002
ASSET_VERSION=

# Email
EMAIL_PROVIDER=disabled
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM="no-reply@example.com"
MAIL_TO="hello@example.com"

# Stripe (optional)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# DB (optional / only if needed)
DATABASE_URL=
